<!-- Navigation icon: KeeLoq brute-force screen (IC chip with pins) -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">

    <!-- Chip body (center square) -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M7,7h10v10H7z"/>

    <!-- Top pins (two small rects protruding upward) -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M9,3h2v3H9zM13,3h2v3h-2z"/>

    <!-- Bottom pins (two small rects protruding downward) -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M9,18h2v3H9zM13,18h2v3h-2z"/>

    <!-- Left pins (two small rects protruding left) -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M3,9h3v2H3zM3,13h3v2H3z"/>

    <!-- Right pins (two small rects protruding right) -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M18,9h3v2h-3zM18,13h3v2h-3z"/>

    <!-- Inner core dot (small center square) -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M10,10h4v4h-4z"/>

</vector>