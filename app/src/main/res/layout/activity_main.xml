<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main content area - acts as the blur source for the nav drawer overlay -->
    <eightbitlab.com.blurview.BlurTarget
        android:id="@+id/blur_target"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="?attr/colorSurface">

            <!-- Top toolbar with nav drawer toggle and app title -->
            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorSurface"
                app:titleTextColor="?attr/colorOnSurface"
                app:navigationIconTint="?attr/colorPrimary"
                app:title="@string/app_name" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="12dp"
                android:paddingVertical="6dp"
                android:background="?attr/colorSurfaceVariant">

                <!-- BLE connection status message -->
                <TextView
                    android:id="@+id/ble_status_text"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Not connected"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSurfaceVariant" />

                <!-- Device selector, visible once scan finds devices -->
                <Spinner
                    android:id="@+id/device_spinner"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:visibility="gone" />

                <!-- Scan/connect/disconnect BLE button -->
                <Button
                    android:id="@+id/ble_button"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:text="Scan BLE"
                    android:textSize="11sp"
                    android:minWidth="80dp"
                    style="@style/TextButton"
                    android:textColor="?attr/colorPrimary"
 />
            </LinearLayout>

            <!-- Host for all navigation fragments -->
            <FrameLayout
                android:id="@+id/fragment_container"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="?attr/colorOutline" />

            <!-- Tappable header to expand/collapse the log panel -->
            <TextView
                android:id="@+id/log_toggle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="&#9654; Log"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:background="?attr/colorSurfaceVariant"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:clickable="true"
                android:focusable="true" />

            <!-- Collapsible scrollable log output panel -->
            <ScrollView
                android:id="@+id/log_scroll"
                android:layout_width="match_parent"
                android:layout_height="180dp"
                android:visibility="gone"
                android:background="?attr/colorSurface"
                android:padding="4dp">

                <TextView
                    android:id="@+id/log_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textSize="11sp"
                    android:fontFamily="monospace"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:textIsSelectable="true" />
            </ScrollView>
        </LinearLayout>
    </eightbitlab.com.blurview.BlurTarget>
    <!-- Nav drawer container: blurred glass overlay + navigation menu -->
    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start">
        <!-- Blurred background layer for the drawer glass effect -->
        <eightbitlab.com.blurview.BlurView
            android:id="@+id/blur_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#991E1E1E" />

        <!-- Navigation menu drawn on top of blur layer -->
        <com.google.android.material.navigation.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@android:color/transparent"
            app:itemTextColor="?attr/colorOnSurfaceVariant"

                        app:headerLayout="@layout/nav_header"
            app:menu="@menu/nav_drawer" />
    </FrameLayout>
</androidx.drawerlayout.widget.DrawerLayout>
